<h1><%= @group.name %></h1>
			    <%= @group.description %> <br>
			    <%= @group.time %> <br>
			    <%= @group.location %> <br>
<%= button_to "Join this group", join_path(id: @group.id), :class => "button medium", :method => :patch %>